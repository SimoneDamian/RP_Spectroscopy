When the user presses the "Add Reference Line" in the Reference lines page (@reference_lines_page.py), a new page has to be opened (always in the left part of the qsplit of the @laser_page.py). This page has to be structured as follows (from the top to the bottom): a copy of the same functionalities and structure of the ScanPage (@laser_controller_page.py). Below this I want: title "Save reference line", below this, 8 rows with the texts: "Name", "Board", "Scan center", "Reference line left side", "Reference line right side", "Lock Region Min", "Lock Region Max" and "Polarity".On the right of all these names, but the "Board" one, I want a textbox in which the user can insert data (strings for "Name" and "Polarity" and floats for the others). Below these rows I want a plot panel like the one of the reference lines page. In this panel I want to plot 1)The plot of the scan (all the scan plots are currently shown on the right part of the window in the plot panel) at the voltage inserted in the "Scan center" textbox (or the closest one, due to decimal numbers with many terms) (using a method like the get_sweep_from_scan in @LaserLockingController_new.py to access the scan that is already available in the GUI) 2) Two vertical red dashed lines at "Reference line left side" and "Reference line right side" 3) Two vertical red lines at "Lock Region Min" and "Lock Region Max" with a semitransparent (alpha = 60) red region in between them. Under the plot the "Save reference line button" and under that the "back button". The "Back" button makes the page turn back to the previous page and the "Save reference line" one for now does nothing.
