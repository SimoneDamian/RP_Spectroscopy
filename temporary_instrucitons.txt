----- Scan page instructions -----
I want to populate the "Scan" page inside the @laser_controller_page.py left stack. From the top to the bottom of the page. A row with three textboxes for the user to insert the "Start voltage", "End voltage" and the "Number of points". Below these I want, in the same row, the "Start scan" and the "Stop scan" buttons. At the bottom of the page, as for the other pages in the stack, I want the "Back" button.
The user fills the three textboxes and presses the "Start scan" button. This triggers the start_scan method (already implemented) of the LaserManager in @laser_manager.py. This changes the state of the Finite State Machine (already implemented) that sends a packet labeled with the "mode":"SCAN" and that is a dictionary structured as follows: packet = {"mode": "SCAN", "step_index": self.scan_index, "total_steps": len(self.scan_voltages), "current_voltage": target_v, "scan_data": self.scan_results, # Sends all accumulated data "latest_sweep": current_sweep   # Sends just the newest trace}. When the "Start scan" button is pressed, I want the "Start scan" and the "Back" buttons to be deactivated. When, during the scan, the user presses the "Stop scan" button, this triggers the stop_scan method in @laser_manager.py that changes the state of the Finite State Machine to "IDLE" (already implemented) and then the "Start sweep" and the "Back" buttons have to be re-enabled. When the "Back" button is pressed, the left part of the window must return to the main menu and the start_sweep method inside @laser_manager.py has to be triggered.
When the GUI receives the signal with the package with the "mode":"SCAN", the visualization of the plot panel (details in @plot_panel.py and implementation in the page in @laser_controller_page.py) has to look like this: A scrollable element organized as a column of plots of the "scan_data" of the different "current_voltage" of the scan. In each single plot I want as title the "current_voltage" and the plot of the "scan_data" (that contains "x" and "error_signal"). Each time the GUI receives a new package it has to plot only the last plot, keeping the old one visualized. At the bottom of the page I want a status bar that shows to the user the percentage of the scan (using the "step_index" and the "total_steps" entries of the package).

----- New parameters page instructions -----
I modified the structure of the writeable parameters in the .yaml file that contains the parameters of the Red Pitaya (you can find an example in @RedPitaya_K_parameters copy.yaml) and I want to update the load_RedPitaya_parameters method inside the @interface.py in order to read it correctly (I simply added more entries for each parameter). The entries are: hardware_name, gui_name, description that are strings, type that can be int, float or bool, initial_value, min, max, scaling that are numbers of the type aforementioned or can also be null and units that can be a string or can be null.
I also want to change the visualization of these parameters table in the GUI. The page is the ParametersPage class in the @laser_controller_page.py and the parameters are loaded in the table using the load_parameters method using the entries of the dictionary of the writeable parameters. In this new version I want to have, for each row, these columns: ["Name", "Value", "Units", "Min", "Max"] that are linked to the entries of the dictionary elements [gui_name, value, units, min, max]. As it is now, only the "Value" column is editable by the user.
In addition, I want the user to be able to see the "description" of a parameter when the arrow stays on it.

AGGIUNGERE AGGIORNAMENTO PARAMETRI ALLA CHIUSURA E DEFAULT SETTINGS CHE VANNO CAMBIATI DI CONSEGUENZA

ATTENZIONE A SCRITTURA PARAMETRI SU RP, TENERE COME ULTIMA COSA

----- New advanced settings page instructions -----
I rewritten the advanced settings part of the .yaml file linked to the specific board one connects to (have a look at @RedPitaya_K_parameters copy.yaml). This file is red in the load_default_advanced_settings method contained in the service_manager.py file. I want you to check if with these new modifications the file can still be red.
I want to modify the visualization of these settings in the @advanced_settings_page.py. I still want a scrollable list of QGroupBox widgets, one per ui_label group found in the advanced_settings section of the board YAML but now with a specific format for each group. For the unlock_detection group: the title is the ui_label one. Then I want a drop-down menu for each one of the other elements (events, fast_control, slow_control, monitor_signal), each one named by its gui_name. Inside this drop-down menu I want another drop-down menu for each entry with the associated gui_name. Inside this I want a row with the other informations like a checkbox for the enabled status and, where it is present, a textbox for the threshold or for the num_points. As before, the only thing that the user is allowed to modify is the enabled status, the num_points and the threshold entries.
For the gui_visualization group: The title of the group is the ui_label. For each other element (history_length, fast_control_signal_derivative, slow_control_signal_derivative) I want a drop-down menu labeled with the associated gui_name that contains a checkbox for the enabled entries and a textbox for the fast and slow history_lengths (followed by the units). Only the fast, slow and enabled entries can be modified by the user.
For the autolock_settings group: The title of the group is the ui_label. In this case I want only two rows, one for mode and one for determine_offset, each one with its gui_name. Then, in the mode row I want a dropdown menu from which the user can choose between the available options. For the determine_offset I want only a checkbox for the enabled value.
For the autocenter_settings group: The title of the group is the ui_label. For each element (jitter_threshold, threshold_count, offset_big_jump, offset_small_jump, offset_try_list, correlation_minimum, length_match_minimum, proportion_free_space_left) I want a drop-down menu labeled with the associated gui_name that contains a textbox where the user can modify the value of the parameter.
In addition, I want the user to be able to see the "description" of a setting when the arrow stays on it.