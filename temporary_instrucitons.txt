I want to create the page that has to open on the left side of the Qsplit in the @laser_controller_page.py when the user clicks on the "Line centering" button. I want the title "Line centering", two rows with "Start voltage", "Stop voltage" and the relative textbox for the user to insert the desired values (with 0.05 and 1.75 as starting values as it is in the @add_reference_line_page.py). Underneath I want a row with the text "Select the reference line" and a drop-down menu with all the available reference lines names (The general manager @general_manager.py has access to the updated database), when the user selects the desired line name I want a plot underneath with the same style of the @reference_lines_page.py that plots the associated reference line. Under this plot I want a "Start scan" and a "Stop scan" buttons that are together all the page wide (i will implement its functionalities later on). Under this I want another plot that will be filled later on. Under this plot I want a row with "Center here", a textbox, a "?" and a "Center" button. At the bottom of the page I want a "Back" button that is as wide as the page that makes the GUi turn back to the menu where the user came.
